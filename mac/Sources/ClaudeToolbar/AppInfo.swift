import ClaudeToolbarCore

enum AppInfo {
    static let version = CoreInfo.version
    static let bundleIdentifier = "io.github.ahm-adsaad.ClaudeToolbar"
    static let displayName = "Claude Toolbar"
}
